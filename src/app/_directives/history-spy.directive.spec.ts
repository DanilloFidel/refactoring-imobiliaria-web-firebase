import { HistorySpyDirective } from './history-spy.directive';

describe('HistorySpyDirective', () => {
  it('should create an instance', () => {
    const directive = new HistorySpyDirective();
    expect(directive).toBeTruthy();
  });
});
